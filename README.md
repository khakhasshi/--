# 星谕塔罗 (Star Oracle Tarot) 🔮

一个优雅的在线塔罗牌占卜应用，结合了真随机数生成和 AI 智能解读功能，为用户提供专业的塔罗牌占卜体验。

## 📖 项目介绍

### 核心特性
- **完整牌组**：包含78张标准塔罗牌（22张大阿卡纳 + 56张小阿卡纳）
- **真随机抽牌**：使用澳大利亚国立大学量子随机数生成器，确保抽牌的真实随机性
- **AI 智能解读**：集成 DeepSeek AI API，根据用户问题和抽取的牌组提供专业解读
- **精美动画效果**：
  - 星空背景动画
  - 洗牌动画效果
  - 翻牌动画效果
  - 卡牌悬浮效果
- **逆位支持**：随机出现逆位牌，提供更丰富的解读维度
- **自定义抽牌数**：支持1-10张牌的灵活抽取
- **响应式设计**：适配各种屏幕尺寸

### 技术栈
- **前端**：HTML5、CSS3、JavaScript (原生)
- **后端**：Node.js + Express
- **AI 解读**：DeepSeek Chat API
- **随机数源**：ANU Quantum Random Numbers API
- **字体**：Cinzel (标题) + Lato (正文)

### 文件结构
```
抽牌/
├── index.html          # 页面结构和主要布局
├── style.css           # 样式表（包含动画效果）
├── script.js           # 前端逻辑（抽牌、动画控制）
├── server.js           # Express 服务器（API 代理）
├── package.json        # 项目依赖配置
├── favicon.svg         # 网站图标
└── README.md          # 项目文档
```

## 🚀 部署步骤

### 前置要求
- Node.js (v14 或更高版本)
- npm (通常随 Node.js 一起安装)
- DeepSeek API Key

### 本地开发部署

#### 1. 安装依赖
```bash
cd /Users/jingzhejiang/Desktop/抽牌
npm install
```

#### 2. 配置 API Key（可选）
如果需要修改 API Key，编辑 `server.js` 文件：
```javascript
const API_KEY = "你的-DeepSeek-API-Key";
```

#### 3. 启动服务器
```bash
npm start
```
或
```bash
node server.js
```

#### 4. 访问应用
打开浏览器访问：
```
http://localhost:7009
```

### 生产环境部署

#### 使用 PM2 部署（推荐）

##### 1. 安装 PM2
```bash
npm install -g pm2
```

##### 2. 启动应用
```bash
pm2 start server.js --name "tarot-app"
```

##### 3. 设置开机自启
```bash
pm2 startup
pm2 save
```

##### 4. 查看运行状态
```bash
pm2 status
pm2 logs tarot-app
```

#### 使用云服务部署

##### Vercel 部署
1. 安装 Vercel CLI：
```bash
npm install -g vercel
```

2. 部署：
```bash
vercel
```

3. 按照提示完成配置

##### Railway 部署
1. 访问 [Railway.app](https://railway.app/)
2. 点击 "New Project" → "Deploy from GitHub repo"
3. 选择你的仓库
4. 添加环境变量（如需要）
5. 点击 "Deploy"

##### 阿里云/腾讯云 ECS 部署
1. 连接到服务器：
```bash
ssh user@your-server-ip
```

2. 克隆项目或上传文件

3. 安装依赖并启动：
```bash
cd /path/to/project
npm install
pm2 start server.js --name "tarot-app"
```

4. 配置 Nginx 反向代理（可选）：
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:7009;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### 环境变量配置

可以通过环境变量配置端口：
```bash
PORT=8080 npm start
```

或创建 `.env` 文件：
```env
PORT=8080
API_KEY=your-deepseek-api-key
```

## 🛑 取消部署步骤

### 停止本地服务器

#### 方法 1：终端中直接停止
在运行 `npm start` 的终端窗口中按 `Ctrl + C`

#### 方法 2：查找并终止进程
```bash
# 查找占用端口的进程
lsof -i :7009

# 终止进程（使用上一步获取的 PID）
kill -9 <PID>
```

### 停止 PM2 管理的服务

#### 停止应用
```bash
pm2 stop tarot-app
```

#### 删除应用
```bash
pm2 delete tarot-app
```

#### 停止所有应用
```bash
pm2 stop all
pm2 delete all
```

#### 取消开机自启
```bash
pm2 unstartup
```

### 取消云服务部署

#### Vercel
```bash
vercel remove tarot-app
```
或在 Vercel Dashboard 中删除项目

#### Railway
1. 登录 Railway Dashboard
2. 选择项目
3. Settings → Danger Zone → Delete Project

#### 阿里云/腾讯云 ECS
```bash
# 停止 PM2 服务
pm2 stop tarot-app
pm2 delete tarot-app

# 删除项目文件
rm -rf /path/to/project

# 如果配置了 Nginx，删除配置文件
sudo rm /etc/nginx/sites-enabled/tarot-app
sudo nginx -s reload
```

## 💡 使用说明

1. **输入问题**：在文本框中输入你想要询问的问题
2. **选择牌数**：选择要抽取的牌数（1-10张）
3. **开始仪式**：点击"开始仪式"按钮
4. **等待洗牌**：观看洗牌动画
5. **翻牌查看**：点击背面朝上的牌进行翻牌
6. **AI 解读**：所有牌翻开后，AI 将根据你的问题进行解读

## 🔧 常见问题

### 服务器无法启动
- 检查端口 7009 是否被占用
- 确保 Node.js 版本正确
- 检查依赖是否正确安装

### AI 解读失败
- 检查网络连接
- 确认 DeepSeek API Key 是否有效
- 查看服务器日志排查错误

### 随机数生成失败
应用会自动降级使用本地伪随机数生成器

## 📝 开发指南

### 修改 API Key
编辑 [server.js](server.js#L7) 中的 `API_KEY` 变量

### 修改端口
编辑 [server.js](server.js#L4) 中的 `port` 变量或使用环境变量

### 自定义样式
修改 [style.css](style.css) 文件

### 添加新功能
在 [script.js](script.js) 中添加前端逻辑

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**享受你的塔罗之旅！✨**
